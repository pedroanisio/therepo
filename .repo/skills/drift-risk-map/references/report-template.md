# Report Template

Use this exact structure for the drift-risk map output. Adapt section
content to the codebase, but preserve the heading hierarchy and the
table schema.

---

```markdown
---
disclaimer: >
  This document was generated by automated static heuristic analysis of a
  codebase snapshot. No statement or finding in this report should be taken
  as ground truth. Every coupling and risk assessment is a hypothesis derived
  from file-pattern matching and structural inference — not from runtime
  verification or formal proof. Findings not backed by a concrete file
  reference or a verifiable logical relationship may be inaccurate or
  incomplete. Validate all CRITICAL and HIGH findings manually before acting
  on them.
---

# Drift-Risk Map — [Project Name]

**Generated**: [ISO 8601 timestamp]
**Scope**: [Full repo | Scoped to: <user-specified scope>]
**Commit / ref**: [HEAD commit SHA if available, or "working tree"]

## Executive Summary

[Total couplings found]. [Count by risk tier: N CRITICAL, N HIGH,
N MODERATE, N LOW].

**Top-3 drift vectors:**

1. [One-line summary of the most dangerous coupling, with file refs]
2. [Second most dangerous]
3. [Third most dangerous]

**Overall posture**: [One paragraph. Is this codebase mostly guarded? Are
the critical paths covered? Is the main risk in the periphery or core?
Honest assessment — do not flatter.]

> A visual overview of this coupling topology is available in
> `drift-risk-map.svg` (generated alongside this report).

## Coupling Inventory

| # | Source Artifact | Dependent Artifact(s) | Coupling Mechanism | Consistency Guard | Propagation Mode | Drift Risk |
|---|---|---|---|---|---|---|
| 1 | `path/to/source` | `path/to/dep1`, `path/to/dep2` | manual-mirror | ⚠ NONE | silent | **CRITICAL** |
| 2 | `path/to/spec.yaml` | `path/to/generated/` | codegen | `make generate` in CI | build-error | LOW |
| ... | ... | ... | ... | ... | ... | ... |

### Coupling mechanism legend

- **codegen**: Dependent is machine-generated from the source. Stays in sync
  if the generation step runs.
- **shared-import**: Dependent imports types/values directly from the source
  module. Compiler/interpreter enforces the contract.
- **manual-mirror**: Dependent restates information from the source by hand.
  No automated enforcement.
- **contract-test**: A dedicated test asserts agreement between source and
  dependent (e.g., schema compatibility test, API contract test).
- **lint-rule**: A linter or static analysis rule enforces consistency.
- **build-step**: A build/compile step fails if the artifacts disagree.
- **runtime-validation**: Inconsistency is caught at runtime (request
  rejection, deserialization error), not at build/test time.
- **none**: No known enforcement mechanism.

### Propagation mode legend

- **build-error**: Change in source causes a build/compile failure.
  Immediate, loud.
- **type-error**: Change causes a type-checker failure (e.g., `tsc`,
  `mypy`, `pyright`). Loud if type-checking is in CI.
- **test-failure**: Change causes an existing test to fail. Loud if tests
  run in CI.
- **lint-failure**: Change causes a linter to flag. Loud if linting is in CI.
- **runtime-error**: Change causes an error at runtime (500, crash, bad
  response). Loud only in production or integration tests.
- **silent**: Change propagates with no error at any stage. The dependent
  artifact is now wrong and nothing signals this.

## CRITICAL Findings

### [Finding #N]: [Short title]

**Source**: `path/to/source:L<line>` — [what it defines]
**Dependent**: `path/to/dependent:L<line>` — [what it restates/mirrors]
**Mechanism**: [manual-mirror | none | ...]
**Guard**: ⚠ NONE

**How drift manifests**: [Concrete scenario. "If a developer adds field
`foo` to `models/user.py`, the serializer at `api/serializers.py:42` will
silently omit it from API responses. No test covers this path because..."]

**Recommended fix**: [Specific. Name the tool, the check, the CI step.
E.g., "Add a `pytest` parametrized test that introspects `User` model
fields and asserts each appears in `UserSerializer.Meta.fields`. Run in
CI on every PR."]

---

*(Repeat for each CRITICAL finding)*

## HIGH Findings

### [Finding #N]: [Short title]

**Source**: `path/to/source`
**Dependent**: `path/to/dependent`
**Mechanism**: [...]
**Guard**: [What exists, and why it's insufficient]

**Gap**: [What scenario slips through the existing guard.]
**Upgrade path**: [What would demote this to MODERATE or LOW.]

---

*(Repeat for each HIGH finding)*

## Methodology Notes

### What was inspected

- [List of directories/paths scanned]
- [Languages/frameworks detected]
- [CI configuration files read]

### What was NOT inspected

- [Anything out of scope: e.g., external services, private packages,
  runtime behavior, deploy-time config injection]
- [Known limitations: e.g., dynamic imports not traced, runtime-only
  coupling not detectable statically]

### Heuristic limitations

This analysis is based on static file-pattern matching (grep, find, AST
scanning where feasible). It cannot detect:

- Couplings mediated by runtime reflection or metaprogramming.
- Couplings that exist only across repository boundaries (multi-repo
  setups) unless all repos are present.
- Semantic equivalence (two definitions may look different but be
  logically identical).
- The completeness of any test suite — a "test-failure" propagation
  mode assumes the relevant test exists and covers the relevant path.
```

---

## Notes on adapting the template

- If the user has specified formatting preferences (e.g., frontmatter
  disclaimer style), incorporate them into the header.
- If the codebase is small (< 5 couplings), the inventory table alone may
  suffice — skip the detailed CRITICAL/HIGH sections if there is nothing
  to detail.
- If the codebase is very large, consider splitting the inventory by
  domain (one table per coupling domain) for readability.
- Always preserve the methodology section — it is the report's
  epistemic hygiene.
