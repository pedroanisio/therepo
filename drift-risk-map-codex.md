---
disclaimer: >
  This document was generated by automated static heuristic analysis of a
  codebase snapshot. No statement or finding in this report should be taken
  as ground truth. Every coupling and risk assessment is a hypothesis derived
  from file-pattern matching and structural inference, not from runtime
  verification or formal proof. Findings not backed by a concrete file
  reference or a verifiable logical relationship may be inaccurate or
  incomplete. Validate all HIGH findings manually before acting on them.
generated_by: "Codex GPT-5"
date: "2026-04-01"
---

# Drift-Risk Map - repo (therepo)

**Generated**: 2026-04-01T00:00:00-03:00
**Scope**: Full repo
**Commit / ref**: `7c36240c5fe251c317a147806fbc84529fa63797` (working tree)

## Executive Summary

9 couplings found. 0 CRITICAL, 5 HIGH, 2 MODERATE, 2 LOW.

**Top-3 drift vectors:**

1. `AGENTS.md` publishes the wrong machine-readable schema for `repo --json`, while the file also instructs agents to parse that output programmatically.
2. The repo ships seven embedded prompt defaults in code, but the checked-in `.repo/prompts/` mirror only contains six files and omits `audit-repo.md`.
3. Human-facing command inventories in `README.md`, `docs/quickstart.md`, and `AGENTS.md` manually mirror the clap command surface and are already inconsistent about `completions` and built-in plugin discovery.

**Overall posture**: The codebase is strongest where drift has been converted into tests or compile-time embedding. `include_str!` and `include_bytes!` make the binary loud when embedded assets disappear, and a focused regression suite already checks several doc-count invariants. The weak zone is still manual documentation mirrors: `AGENTS.md`, `README.md`, `docs/quickstart.md`, and the checked-in `.repo/prompts/` directory all restate runtime behavior or embedded asset inventories by hand. Those mirrors are only partially guarded, and several are already stale in the current tree.

> A visual overview of this coupling topology is available in
> `drift-risk-map-codex.svg` (generated alongside this report).

## Coupling Inventory

| # | Source Artifact | Dependent Artifact(s) | Coupling Mechanism | Consistency Guard | Propagation Mode | Drift Risk |
|---|---|---|---|---|---|---|
| 1 | `crates/repo-cli/src/commands/overview.rs:7`, `crates/repo-cli/src/commands/overview.rs:32` | `AGENTS.md:90` | manual-mirror | ⚠ NONE | runtime-error | **HIGH** |
| 2 | `crates/repo-cli/src/commands/overview.rs:57`, `crates/repo-cli/tests/cli.rs:63` | `AGENTS.md:62` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 3 | `crates/repo-cli/src/plugin/mod.rs:43` | `AGENTS.md:315` | manual-mirror | ⚠ NONE | runtime-error | **HIGH** |
| 4 | `crates/repo-cli/src/plugin/builtin/skills.rs:702`, `crates/repo-cli/src/plugin/builtin/skills.rs:732` | `crates/repo-cli/README.md:134` | manual-mirror | Partial regression checks in `crates/repo-cli/src/plugin/builtin/skills.rs:2542` | silent | **MODERATE** |
| 5 | `crates/repo-cli/src/plugin/builtin/prompt.rs:31` | `.repo/prompts/*` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 6 | `crates/repo-cli/src/cli.rs:74` | `README.md:99`, `docs/quickstart.md:43` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 7 | `crates/repo-cli/src/plugin/builtin/skills.rs:2542` | `AGENTS.md:229`, `docs/quickstart.md:90`, `docs/how-to-bootstrap-repo-metadata.md:40`, `crates/repo-cli/README.md:138` | contract-test | `cargo llvm-cov` in `.github/workflows/coverage.yml:1` | test-failure | **LOW** |
| 8 | `crates/repo-cli/src/cli.rs:114` | `docs/cli-reference.md:48` | contract-test | `crates/repo-cli/src/plugin/builtin/skills.rs:2647` | test-failure | **MODERATE** |
| 9 | `crates/repo-cli/src/plugin/builtin/prompt.rs:31` | `crates/repo-cli/tests/prompt_ulid_cli.rs:67` | contract-test | `cargo llvm-cov` in `.github/workflows/coverage.yml:1` | test-failure | **LOW** |

### Coupling mechanism legend

- **codegen**: Dependent is machine-generated from the source. Stays in sync if the generation step runs.
- **shared-import**: Dependent imports types or values directly from the source module. Compiler or interpreter enforces the contract.
- **manual-mirror**: Dependent restates information from the source by hand. No automated enforcement.
- **contract-test**: A dedicated test asserts agreement between source and dependent.
- **lint-rule**: A linter or static analysis rule enforces consistency.
- **build-step**: A build or compile step fails if the artifacts disagree.
- **runtime-validation**: Inconsistency is caught at runtime, not at build or test time.
- **none**: No known enforcement mechanism.

### Propagation mode legend

- **build-error**: Change in source causes a build or compile failure.
- **type-error**: Change causes a type-checker failure.
- **test-failure**: Change causes an existing test to fail.
- **lint-failure**: Change causes a linter to flag.
- **runtime-error**: Change causes an error at runtime.
- **silent**: Change propagates with no error at any stage.

## CRITICAL Findings

No CRITICAL couplings were identified under the skill's strict definition of "no guard plus silent propagation." The most dangerous vectors in this repo are HIGH: unguarded manual documentation mirrors and a stale checked-in prompt mirror.

## HIGH Findings

### Finding #1: `AGENTS.md` advertises the wrong `repo --json` schema

**Source**: `crates/repo-cli/src/commands/overview.rs:7` and `crates/repo-cli/tests/cli.rs:93`
**Dependent**: `AGENTS.md:90`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The code emits `{name,builtin_plugins,external_plugins,config_present}`, but `AGENTS.md` documents `{name,docs,plugins,config}` and explicitly tells agents to parse JSON programmatically. An agent or script written to the documented shape will break when pointed at the real binary.
**Upgrade path**: Add a regression test that loads `AGENTS.md` and asserts that the documented JSON keys match the serialized `OverviewJson` shape, or generate the JSON example directly from a fixture run of `repo --json --plain`.

---

### Finding #2: `AGENTS.md` claims bootstrap auto-creates `.repo/config.toml`

**Source**: `crates/repo-cli/src/commands/overview.rs:57` and `crates/repo-cli/tests/cli.rs:63`
**Dependent**: `AGENTS.md:62`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The current bootstrap sequence creates `.repo/storage/.keep`, but neither `repo --json` nor the init commands shown in `AGENTS.md` create `.repo/config.toml`. The guide promises an artifact the workflow does not produce.
**Upgrade path**: Either remove `config.toml` from the bootstrap tree in `AGENTS.md`, or add a dedicated `repo config init` or equivalent generator and test it in CI.

---

### Finding #3: `AGENTS.md` treats `plugins` and `completions` as built-in plugins

**Source**: `crates/repo-cli/src/plugin/mod.rs:43` and `crates/repo-cli/src/cli.rs:74`
**Dependent**: `AGENTS.md:315`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The clap command surface has seven top-level commands, but plugin discovery only registers five built-in plugins: `docs`, `health`, `skills`, `prompt`, and `ulid`. `AGENTS.md` collapses those two concepts and documents seven built-in plugins, which is not what `repo plugins --json` returns.
**Upgrade path**: Document commands and discovered plugins separately, and add a doc test that compares the built-in plugin names in `AGENTS.md` against `discover_plugins()`.

---

### Finding #4: Checked-in `.repo/prompts/` silently lags the embedded prompt defaults

**Source**: `crates/repo-cli/src/plugin/builtin/prompt.rs:31`
**Dependent**: `.repo/prompts/*`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The embedded defaults include `audit-repo.md`, but the checked-in `.repo/prompts/` directory currently contains only six prompt files and omits that prompt. Nothing in CI verifies that the checked-in mirror matches the embedded runtime defaults.
**Upgrade path**: Add a test that diffs `DEFAULTS` against the repo's `.repo/prompts/` filenames, or stop checking in `.repo/prompts/` as a mirror and treat `repo prompt init` as the sole distribution path.

---

### Finding #5: Command inventories in human docs omit `completions`

**Source**: `crates/repo-cli/src/cli.rs:74`
**Dependent**: `README.md:99` and `docs/quickstart.md:43`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The CLI exposes `Completions`, but both docs enumerate the built-in command list without it. This is classic silent drift: the binary grows, but the user-facing inventory does not.
**Upgrade path**: Add a regression test that parses the top-level clap command list and checks the documentation inventories for the same set of names, including `completions`.

## Methodology Notes

### What was inspected

- Repo topology under `crates/repo-cli/`, `docs/`, `_docs/`, `.repo/`, `.github/workflows/`, and `scripts/`
- Rust source defining the clap interface, overview JSON, plugin discovery, prompt defaults, and skill initialization
- Regression tests under `crates/repo-cli/tests/`
- GitHub Actions workflows `coverage.yml` and `release.yml`
- Runtime spot checks with isolated temp repos using `repo --json --plain`, `repo skills init`, `repo prompt init`, and `repo plugins --json --plain`

### What was NOT inspected

- External services, GitHub releases, or crates.io metadata beyond what is checked into the repo
- Runtime behavior behind future "Phase 2" plugin dispatch paths
- Cross-repo consumers that may parse `repo` output from outside this repository

### Heuristic limitations

This analysis is based on static file-pattern matching and direct source reads. It cannot detect:

- Couplings that exist only in downstream repos or unpublished release artifacts
- Semantic equivalence where docs are phrased differently but remain functionally correct
- Runtime-only drift that depends on shell environment or external registry behavior
- Whether all existing tests run in every local workflow outside the checked-in CI configuration
