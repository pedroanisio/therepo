---
disclaimer: >
  This document was generated by automated static heuristic analysis of a
  codebase snapshot. No statement or finding in this report should be taken
  as ground truth. Every coupling and risk assessment is a hypothesis derived
  from file-pattern matching and structural inference — not from runtime
  verification or formal proof. Findings not backed by a concrete file
  reference or a verifiable logical relationship may be inaccurate or
  incomplete. Validate all CRITICAL and HIGH findings manually before acting
  on them.
generated_by: "Claude Opus 4.6 via Claude Code"
date: "2026-04-01"
---

# Drift-Risk Map — repo (therepo)

**Generated**: 2026-04-01T00:00:00Z
**Scope**: Full repo
**Commit / ref**: `7c36240c5fe251c317a147806fbc84529fa63797` (branch `rel/v0.4.0-rc-1`)

## Executive Summary

16 couplings found. 3 CRITICAL, 9 HIGH, 2 MODERATE, 2 LOW.

**Top-3 drift vectors:**

1. **Skill count in docs vs. code** — Code and disk have 14 built-in skills, but `AGENTS.md`, `quickstart.md`, `how-to-bootstrap-repo-metadata.md`, and `crates/repo-cli/README.md` all say 12. Active drift.
2. **Prompt count in AGENTS.md vs. code** — 7 prompts on disk and embedded in code, but `AGENTS.md` says 6 in three separate locations. Active drift.
3. **AGENTS.md skill table vs. actual skill inventory** — The skill table in AGENTS.md lists 12 skills and omits `response-dispatch`, `drift-risk-map`, `cli-ux-patterns`, and `codebase-requirements` (partially — `cli-ux-patterns` and `codebase-requirements` are listed, but `response-dispatch` and `drift-risk-map` are not). Active drift.

**Overall posture**: This codebase has strong automated guards at the code level — clippy, tests, and 91% coverage threshold run in CI and pre-commit hooks. However, the primary risk surface is the **manual-mirror coupling between embedded asset inventories/CLI definitions and human-readable documentation**. Every time a new skill, prompt, or reference is added to `defaults/`, the count and listing must be updated in 5–7 separate documentation files by hand. Similarly, every new CLI subcommand or flag must be manually reflected in `docs/cli-reference.md`, `AGENTS.md`, and `crates/repo-cli/README.md`. There is no automated check enforcing either. The drift is already active: the most recent additions (`response-dispatch`, `drift-risk-map`) bumped the code to 14 skills and 7 prompts, but the docs were not fully updated. The crate README is the most stale artifact, missing `skills fix`, `skills deploy`, `health fix`, `plugins info`, and several `docs` listing flags. This is the classic "silent documentation rot" pattern — the crate README in particular is 2 release candidates behind.

> A visual overview of this coupling topology is available in
> `drift-risk-map.svg` (generated alongside this report).

## Coupling Inventory

| # | Source Artifact | Dependent Artifact(s) | Coupling Mechanism | Consistency Guard | Propagation Mode | Drift Risk |
|---|---|---|---|---|---|---|
| 1 | `defaults/skills/` (14 files) | `AGENTS.md:69,228,411` | manual-mirror | ⚠ NONE | silent | **CRITICAL** |
| 2 | `defaults/skills/` (14 files) | `docs/quickstart.md:90`, `docs/how-to-bootstrap-repo-metadata.md:40` | manual-mirror | ⚠ NONE | silent | **CRITICAL** |
| 3 | `defaults/prompts/` (7 files) | `AGENTS.md:72,280,436` | manual-mirror | ⚠ NONE | silent | **CRITICAL** |
| 4 | `defaults/skills/` (14 files) | `crates/repo-cli/README.md:129` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 5 | `Cargo.toml:version` (`0.4.0`) | `CHANGELOG.md` headers | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 6 | `src/cli.rs` (clap definitions) | `docs/cli-reference.md`, `AGENTS.md` §3 | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 7 | `defaults/skills/*.md` (content) | `defaults/skills.toml` (template) | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 8 | `cli.rs:184-197` (`skills` subcmds) | `crates/repo-cli/README.md:117-123` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 9 | `cli.rs:167-174` (`health fix`) | `crates/repo-cli/README.md:83-93` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 10 | `cli.rs:114-136` (`docs` flags) | `docs/cli-reference.md:57-59`, `crates/repo-cli/README.md:60-67` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 11 | `cli.rs:247-256` (`plugins info`) | `crates/repo-cli/README.md:141-147` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 12 | `Cargo.toml:version` (`0.4.0`) | `defaults/schemas/*-prompt-schema.ts:823,825,1172,1174` | manual-mirror | ⚠ NONE | silent | **HIGH** |
| 13 | `src/plugin/builtin/skills.rs` (embed list) | `defaults/skills/` (files on disk) | build-step | `include_str!`/`include_bytes!` compile error | build-error | **LOW** |
| 14 | `src/plugin/builtin/prompt.rs` (embed list) | `defaults/prompts/` (files on disk) | build-step | `include_str!` compile error | build-error | **LOW** |
| 15 | `src/cli.rs` + `src/commands/` (command names) | `tests/cli.rs`, `tests/skills_cli.rs`, etc. | test-assertion | pre-commit + CI test gate | test-failure | **MODERATE** |
| 16 | `.github/workflows/release.yml` (`CRATE_MANIFEST`, `BIN_NAME`) | Actual file paths (`crates/repo-cli/Cargo.toml`, binary name `repo`) | manual-mirror | Release workflow would fail | runtime-error | **MODERATE** |

### Coupling mechanism legend

- **codegen**: Dependent is machine-generated from the source. Stays in sync if the generation step runs.
- **shared-import**: Dependent imports types/values directly from the source module. Compiler/interpreter enforces the contract.
- **manual-mirror**: Dependent restates information from the source by hand. No automated enforcement.
- **contract-test**: A dedicated test asserts agreement between source and dependent (e.g., schema compatibility test, API contract test).
- **lint-rule**: A linter or static analysis rule enforces consistency.
- **build-step**: A build/compile step fails if the artifacts disagree.
- **runtime-validation**: Inconsistency is caught at runtime (request rejection, deserialization error), not at build/test time.
- **none**: No known enforcement mechanism.

### Propagation mode legend

- **build-error**: Change in source causes a build/compile failure. Immediate, loud.
- **type-error**: Change causes a type-checker failure. Loud if type-checking is in CI.
- **test-failure**: Change causes an existing test to fail. Loud if tests run in CI.
- **lint-failure**: Change causes a linter to flag. Loud if linting is in CI.
- **runtime-error**: Change causes an error at runtime. Loud only in production or integration tests.
- **silent**: Change propagates with no error at any stage. The dependent artifact is now wrong and nothing signals this.

## CRITICAL Findings

### Finding #1: Skill count in AGENTS.md (and other docs) says 12, code has 14

**Source**: `crates/repo-cli/defaults/skills/` — 14 files on disk (10 `.md` + 4 `.skill` ZIPs)
**Dependent**: `AGENTS.md:69` ("12 skills"), `AGENTS.md:228` ("12 built-in skills:"), `AGENTS.md:411` ("Skills (12)")
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**How drift manifests**: A developer reads AGENTS.md and sees 12 skills listed. They run `repo skills deploy` and 14 skills are deployed. The table in AGENTS.md §3.4 lists only 12 skill names — `response-dispatch` and `drift-risk-map` are missing entirely. An AI agent following AGENTS.md as its reference will have an incomplete understanding of available skills.

**Recommended fix**: Add a CI check (e.g., a shell script in `.github/workflows/coverage.yml`) that counts the entries in `BUILTIN_SKILLS` + `BUILTIN_SKILL_ZIPS` arrays in `skills.rs` and greps for the count in all documentation files. Alternatively, generate the skill table from the code via a `repo skills list --json` command piped through a template, and fail CI if the docs diverge. Simplest version:

```bash
#!/usr/bin/env sh
SKILL_COUNT=$(grep -c 'filename:' crates/repo-cli/src/plugin/builtin/skills.rs | head -1)
for f in AGENTS.md docs/quickstart.md docs/how-to-bootstrap-repo-metadata.md; do
  if ! grep -q "$SKILL_COUNT" "$f"; then
    echo "ERROR: $f has stale skill count" && exit 1
  fi
done
```

---

### Finding #2: Skill count in quickstart.md and bootstrap guide says 12

**Source**: `crates/repo-cli/defaults/skills/` — 14 files on disk
**Dependent**: `docs/quickstart.md:90` ("all 12 built-in skills"), `docs/how-to-bootstrap-repo-metadata.md:40` ("all 12 built-in skills")
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**How drift manifests**: A user following the quickstart guide reads "Installs all 12 built-in skills" but actually gets 14. While harmless in this direction (more is better), it signals unmaintained documentation and erodes trust. If a skill is later *removed*, the docs would over-promise.

**Recommended fix**: Same CI check as Finding #1. These files should be included in the same automated count verification.

---

### Finding #3: Prompt count in AGENTS.md says 6, code has 7

**Source**: `crates/repo-cli/defaults/prompts/` — 7 files on disk (`audit-repo.md` added in 0.4.0-rc-2)
**Dependent**: `AGENTS.md:72` ("6 prompts"), `AGENTS.md:280` ("6 built-in prompts:"), `AGENTS.md:436` ("Prompts (6)")
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**How drift manifests**: An AI agent reads AGENTS.md and sees 6 prompts listed. It calls `repo prompt list` and gets 7 results. The `audit-repo` prompt is not listed in the AGENTS.md table, so the agent doesn't know it exists and will never suggest it. This is a functional capability gap caused by documentation drift.

**Recommended fix**: Extend the CI count check to cover prompts. Count `include_str!` entries in `prompt.rs` and verify the number appears in AGENTS.md. Also verify the prompt name table is complete.

---

## HIGH Findings

### Finding #4: Crate README lists 12 skills, missing 2

**Source**: `crates/repo-cli/defaults/skills/` (14 files)
**Dependent**: `crates/repo-cli/README.md:129` — lists 12 skills by name
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The crate README skill inventory table was last updated to 12 skills. The two newest skills (`response-dispatch`, `drift-risk-map`) are not listed. This is the same root cause as Findings #1–2.
**Upgrade path**: Include `crates/repo-cli/README.md` in the same CI count/name verification check.

---

### Finding #5: Cargo.toml version ↔ CHANGELOG.md headers

**Source**: `crates/repo-cli/Cargo.toml:3` — `version = "0.4.0"`
**Dependent**: `CHANGELOG.md` headers (`[0.4.0-rc-3]`, `[0.4.0-rc-1]`, etc.)
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE — no `release-please`, `semantic-release`, or `standard-version` automation detected

**Gap**: The CHANGELOG is manually maintained. When cutting a release, there is nothing preventing the Cargo.toml version from being bumped without a corresponding CHANGELOG entry, or vice versa. The version `0.4.0` in Cargo.toml is a base version; the CHANGELOG uses release-candidate suffixes (`0.4.0-rc-3`) that don't match. This is intentional (pre-release cycle) but creates a window where the mapping is unclear.
**Upgrade path**: Add a CI check that verifies the top CHANGELOG header contains the Cargo.toml version string (allowing `-rc-N` suffixes). Consider adopting `cargo-release` or `release-please` for automation.

---

### Finding #6: CLI flags/commands ↔ documentation

**Source**: `crates/repo-cli/src/cli.rs` (clap derive definitions)
**Dependent**: `docs/cli-reference.md`, `AGENTS.md` §3
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: Every CLI flag, subcommand, and help string defined in `cli.rs` is manually restated in two documentation files. The `cli-reference.md` does document the `fix` subcommand for `repo health` (added in 0.4.0-rc-1), so it is currently in sync. However, the next flag or subcommand added to the clap definition will silently leave the docs stale unless the developer remembers to update both files.
**Upgrade path**: Generate `cli-reference.md` from `repo --help` output in CI (e.g., `repo --help > /tmp/help.txt && diff docs/cli-reference.md /tmp/help.txt`). Alternatively, add a test that runs `repo --help` and asserts the output contains all documented flags.

---

### Finding #7: defaults/skills.toml template ↔ actual skill inventory

**Source**: `crates/repo-cli/defaults/skills/` (14 skills on disk)
**Dependent**: `crates/repo-cli/defaults/skills.toml` — the template written by `repo skills init`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The `skills.toml` template contains only commented-out example entries (generic skills from external sources). It does not reference any of the 14 built-in skills. When a user runs `repo skills init` and then `repo skills`, the built-in skills are invisible in the TOML config — they exist only as files on disk. This is by design (built-ins are not user-configured), but if the template ever needs to enumerate built-ins, it would drift instantly.
**Upgrade path**: If the template is intentionally generic, document that built-in skills are separate from configured skills. If it should list built-ins, generate it from the code.

---

### Finding #8: Crate README missing `skills fix` and `skills deploy` subcommands

**Source**: `crates/repo-cli/src/cli.rs:184-197` — defines 6 subcommands: `init`, `export`, `sync`, `install`, `fix`, `deploy`
**Dependent**: `crates/repo-cli/README.md:117-123` — lists only `init`, `export`, `sync`, `install`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The `fix` and `deploy` subcommands are absent from the crate README. A user reading only the crate README would not discover these capabilities.
**Upgrade path**: Include in the same CLI-docs freshness check recommended in Finding #6.

---

### Finding #9: Crate README missing `health fix` subcommand

**Source**: `crates/repo-cli/src/cli.rs:167-174` — defines `init`, `export`, `fix`
**Dependent**: `crates/repo-cli/README.md:83-93` — lists only `init`, `export`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: `repo health fix` (added in 0.4.0-rc-1) is not documented in the crate README.
**Upgrade path**: Same CLI-docs freshness check.

---

### Finding #10: `docs` listing flags incomplete in cli-reference.md and crate README

**Source**: `crates/repo-cli/src/cli.rs:114-136` — defines flags: `query`, `--status`, `--sort`, `--limit`, `--details`, `--interactive`, `--json`
**Dependent**: `docs/cli-reference.md:57-59` — lists only `--status`, `--json`; `crates/repo-cli/README.md:60-67` — lists only `--status`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: Five flags (`--sort`, `--limit`, `--details`, `--interactive`, `query` positional arg) are defined in clap but missing from cli-reference.md. The crate README is even more sparse. AGENTS.md (lines 119-129) is the only doc that lists all 7.
**Upgrade path**: Generate flag lists from `repo docs --help` output.

---

### Finding #11: `plugins info` subcommand missing from crate README

**Source**: `crates/repo-cli/src/cli.rs:247-256` — defines `list` and `info { name }`
**Dependent**: `crates/repo-cli/README.md:141-147` — shows `repo plugins` and `repo plugins list` but not `info`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: The `info` subcommand for plugins is not shown in the crate README.
**Upgrade path**: Same CLI-docs freshness check.

---

### Finding #12: Deprecated schema items past their removal deadline

**Source**: `crates/repo-cli/Cargo.toml:3` — version is `0.4.0`
**Dependent**: `crates/repo-cli/defaults/schemas/01KM1YWRFYBBT98WV14WXDKJM4-prompt-schema.ts:823,825,1172,1174`
**Mechanism**: manual-mirror
**Guard**: ⚠ NONE

**Gap**: Four items in the prompt schema are annotated `@deprecated Since v0.3.0. Removal in v0.4.0.` The project is already at version `0.4.0`, so these deprecated items have passed their stated removal deadline but remain in the codebase. This means the deprecation notice is now misleading — either the items should be removed (honoring the promise), or the removal deadline should be updated to a future version.
**Upgrade path**: Add a CI grep check: `grep -r "Removal in v" defaults/schemas/ | grep -v "v$(cargo pkgid | cut -d# -f2)"` — fails if any removal deadline references the current or a past version. Alternatively, remove the items now.

---

## MODERATE Findings

### Finding #15: Test assertions ↔ CLI output

**Source**: `crates/repo-cli/src/` (command implementations)
**Dependent**: `crates/repo-cli/tests/*.rs` (integration tests using `assert_cmd`)
**Mechanism**: test-assertion
**Guard**: Pre-commit hook runs `cargo test`; CI runs coverage gate

**Notes**: The tests assert on specific output strings, command exit codes, and skill deployment counts. These assertions are tightly coupled to the current implementation. However, they are protected by the test suite running in CI and pre-commit, so drift is caught at test-failure time. The risk is MODERATE rather than LOW because some tests assert on specific numbers (e.g., skill counts) that are hardcoded rather than computed.

---

### Finding #16: Release workflow paths ↔ actual file structure

**Source**: `.github/workflows/release.yml` — env vars `CRATE_MANIFEST: crates/repo-cli/Cargo.toml`, `BIN_NAME: repo`, `ASSET_PREFIX: therepo`
**Dependent**: Actual paths `crates/repo-cli/Cargo.toml`, binary name `repo` in Cargo.toml
**Mechanism**: manual-mirror
**Guard**: Release workflow would fail if paths are wrong (runtime-error)

**Notes**: Currently in sync. The risk is MODERATE because a rename of the crate directory would silently leave the release workflow broken until the next tag push, which could be weeks later. The failure would be loud (CI fails) but delayed.

---

## Methodology Notes

### What was inspected

- All files under `crates/repo-cli/src/` (24 Rust source files)
- All files under `crates/repo-cli/defaults/` (36 default asset files)
- All files under `crates/repo-cli/tests/` (5 integration test files)
- CI configuration: `.github/workflows/coverage.yml`, `.github/workflows/release.yml`
- Git hooks: `.githooks/pre-commit`, `.githooks/pre-push`
- Build config: `crates/repo-cli/Cargo.toml`, `dist-workspace.toml`
- Documentation: `README.md`, `AGENTS.md`, `CHANGELOG.md`, `docs/*.md`, `crates/repo-cli/README.md`
- Scripts: `scripts/check-coverage.sh`, `scripts/install.sh`
- Languages/frameworks: Rust (edition 2024), clap 4.5, serde, toml

### What was NOT inspected

- Runtime behavior of the compiled binary
- External service interactions (`npx skills add`)
- Contents of `.skill` ZIP archives (binary blobs; only filenames checked)
- Multi-repo dependencies (this is a single-crate project)
- Dynamic dispatch paths for external plugins (not yet implemented)

### Heuristic limitations

This analysis is based on static file-pattern matching (grep, find, AST
scanning where feasible). It cannot detect:

- Couplings mediated by runtime reflection or metaprogramming.
- Couplings that exist only across repository boundaries (multi-repo
  setups) unless all repos are present.
- Semantic equivalence (two definitions may look different but be
  logically identical).
- The completeness of any test suite — a "test-failure" propagation
  mode assumes the relevant test exists and covers the relevant path.

### Coupling domains ruled out

The following coupling domains from the standard checklist were investigated and found not applicable:

- **API contract ↔ SDK/client codegen**: No OpenAPI, Protobuf, GraphQL, or gRPC specs. This is a CLI tool, not a service.
- **API contract ↔ request/response validation**: No HTTP API. No Zod, Pydantic, or validation schemas.
- **Data models ↔ DB schema/migrations**: No database. No ORM. No migrations.
- **Data models ↔ serializers/DTOs**: Serde derive macros on structs provide automatic serialization — no manual mirror risk.
- **Config/env ↔ runtime consumers**: No `.env` files. No environment variable consumption beyond standard `CARGO_TERM_COLOR`. Config is file-based (`.repo/config.toml`) with explicit parsing.
- **Infra-as-Code ↔ application expectations**: No Terraform, Helm, or Kubernetes. Release workflow paths are covered in Finding #11.
