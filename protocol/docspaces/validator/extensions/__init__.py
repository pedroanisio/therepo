"""Reference DOCSPACES validator extensions.

Each module in this directory exposes a `register(api)` function that
the loader calls with an ExtensionRegistry. See `EXTENSIONS.md` for the
authoring guide.
"""
